\documentclass[a4paper,twocolumn]{article}

\usepackage[fancy]{template}
\usepackage{survival-pack}

\usepackage{euler}

\setup{%
  subject={Topics in Programming Languages},%
  assignment={Deciding associativity in L0},%
  date={June 12, 2013.}%
}
\setupLocation[short=DIKU]%
{Datalogisk institut, Copenhagen University}
\setupAuthor[%
  addendum=\texttt{\{soeholm,oleks\}@diku.dk} %
]{Kristoffer Søholm \& Oleksandr Shturmov}

\newcommand{\powerset}[1]{\ensuremath{\mathcal{P}\p{#1}}}
\newcommand{\mt}[1]{\ensuremath{\mathtt{#1}}}
\newcommand{\ms}[1]{\ensuremath{\mathtt{#1}^*}}
\newcommand{\ch}[1]{\ensuremath{\mathcal{CH}\p{#1}}}

\newcommand\boolt[0]{\texttt{bool}}
\newcommand\realt[0]{\texttt{real}}
\newcommand\chart[0]{\texttt{char}}
\newcommand\intt[0]{\texttt{int}}
\newcommand\aliases[1]{\textrm{aliases}(#1)}
\renewcommand\tilde[0]{{\raise.17ex\hbox{$\scriptstyle\sim$}}}

\newcommand{\lp}[0]{\,\ifmmode \text{\texttt{+}} \else \texttt{+} \fi\,}
\newcommand{\lt}[0]{\,\ifmmode \text{\texttt{*}} \else \texttt{*} \fi\,}
\newcommand{\lm}[0]{\,\ifmmode \text{\texttt{-}} \else \texttt{-} \fi\,}
\newcommand{\lneg}[0]{{}\ifmmode \text{\tilde} \else \tilde \fi{}}

\newcommand{\LOR}[0]{\,\ifmmode \text{\texttt{||}} \else \texttt{||} \fi\,}
\newcommand{\LAND}[0]{\,\ifmmode \text{\texttt{\&\&}} \else \texttt{\&\&} \fi\,}
\newcommand{\LNOT}[0]{\,\ifmmode \text{\texttt{not}} \else \texttt{not} \fi\,}

\newcommand{\LCONCAT}[0]{\,\ifmmode \text{\texttt{concat}} \else \texttt{concat} \fi\,}
\newcommand{\LMIN}[0]{\,\ifmmode \text{\texttt{min}} \else \texttt{min} \fi}
\newcommand{\LMAX}[0]{\,\ifmmode \text{\texttt{max}} \else \texttt{max} \fi}


\renewcommand{\thefootnote}{\fnsymbol{footnote}}

% There's something funny with the font. It makes table cells have small
% padding at the top. The following hack fixes that:
\def\arraystretch{1.2}

\begin{document}

\thispagestyle{first}

\twocolumn[\maketitle

\input{abstract}

\vspace{0.2in}

\input{keywords}

\vspace{0.2in}

]

\input{introduction}

\input{chaining}

\input{rewriting}

\input{lights_test}

\input{references}

\end{document}

